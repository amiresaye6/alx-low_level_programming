#include "main.h"

/**
 * add - adds two intgers and return the result
 * @a: first number to add
 * @b: second number to add
 * Return: returns the sum
 */
int add(int a, int b)
{
	return (a + b);
}
