#include <stdio.h>

/**
 * main - prints the program name evin if it is changed
 * @argc: intger
 * @argv: intger
 * Return: 0
 */
int main(int argc, char const *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
